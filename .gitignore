# macOS
.DS_Store

# pnpm(构建期依赖:esbuild + puppeteer-core)
node_modules/

# 冒烟测试逐步截图(pnpm run smoke)
test-artifacts/

# Claude Code local machine-specific settings (paths, permissions)
.claude/settings.local.json
